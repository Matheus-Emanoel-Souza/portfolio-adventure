import type { TimelineEntry } from '@/types'

/**
 * Vazio de propósito: trajetória (educação/experiência/marcos) ainda não
 * confirmada. TODO: preencher com histórico real, mais recente primeiro.
 * A UI do Quest Log trata o caso vazio sem inventar entradas.
 */
export const timeline: TimelineEntry[] = []
